<div data-controller="chat" class="max-w-3xl mx-auto bg-white rounded-xl shadow-md overflow-hidden">
  <div class="p-6">
    <h2 class="text-2xl font-bold text-gray-800 mb-6">Solid Cable Chat Demo</h2>
    
    <div class="bg-gray-50 rounded-lg p-4 mb-6">
      <h3 class="font-semibold text-gray-700 mb-2">About this demo:</h3>
      <p class="text-gray-600 text-sm mb-4">
        This is a real-time chat application built with Solid Cable.
        Messages are stored in your database and broadcast via WebSockets.
      </p>
      <div class="space-y-2 text-sm text-gray-600">
        <div class="flex items-start">
          <span class="text-green-500 mr-2">✓</span>
          <span>Real-time message broadcasting</span>
        </div>
        <div class="flex items-start">
          <span class="text-green-500 mr-2">✓</span>
          <span>Database-backed (no Redis needed)</span>
        </div>
        <div class="flex items-start">
          <span class="text-green-500 mr-2">✓</span>
          <span>Built with Rails 8 Solid Cable</span>
        </div>
      </div>
    </div>

    <div class="bg-white border border-gray-200 rounded-lg overflow-hidden mb-6">
      <div id="chat-messages" class="h-96 overflow-y-auto p-4 space-y-3">
        <div class="flex justify-center">
          <div class="bg-gray-200 text-gray-700 text-center text-sm py-1 px-3 rounded-full">
            Chat started
          </div>
        </div>
      </div>
      
      <div class="border-t border-gray-200 p-4 bg-gray-50">
        <div class="flex space-x-2">
          <input 
            type="text" 
            id="message-input" 
            placeholder="Type your message..." 
            class="flex-1 px-4 py-2 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent"
            data-action="keypress->chat#sendOnEnter"
          >
          <button 
            data-action="click->chat#sendMessage"
            class="px-6 py-2 bg-blue-600 text-white font-medium rounded-lg hover:bg-blue-700 transition-colors"
          >
            Send
          </button>
        </div>
      </div>
    </div>
  </div>
</div>